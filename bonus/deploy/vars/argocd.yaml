---
argocd_namespace: argocd
argocd_service_name: argocd-server
argocd_service_port: 80
argocd_source_repo_server: "http://{{ gitlab_webservice_name }}.{{ gitlab_namespace }}:{{ gitlab_webservice_port }}"

argocd_source_repo_path: "root/iot.git"

gateway_argocd_hostname: argocd.iot.local
