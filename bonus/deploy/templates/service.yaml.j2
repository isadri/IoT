apiVersion: v1
kind: Service
metadata:
  name: {{ app_service_name }}
  namespace: {{ app_namespace }}
spec:
  type: {{ service_type }}
  selector:
    app: iot
  ports:
    - port: {{ app_service_port }}
      targetPort: {{ app_port }}
