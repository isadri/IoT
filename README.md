# IoT
## Instructions

> [!WARNING]
> These instructions will setup the cluster on your local machine.

1. `python3 -m venv .venv`.
2. `pip install requirements.txt`.
3. `ansible-playbook main.yaml`.
4. To access the application, visit `http://localhost:8081`. And for ArgoCD, visit `http://localhost:8082`.
