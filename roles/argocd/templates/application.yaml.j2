apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ argocd_app_name }}
  namespace: {{ argocd_namespace }}
spec:
  poject: {{ argocd_project }}
  source:
    repoURL: {{ argocd_source_repo_server }}:{{ argocd_source_repo_path }}
    tagetRevision: HEAD
    path: {{ argocd_source_path }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ application_namespace }}
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
